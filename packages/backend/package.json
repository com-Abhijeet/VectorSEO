{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "src/index.ts",
  "scripts": {
    "dev": "nodemon --watch src --ext ts --exec ts-node src/index.ts --",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist",
    "build:ts": "tsc",
    "copy:assets": "cpx \"src/**/*.{hbs,png,jpg}\" dist",
    "build": "npm run clean && npm run build:ts && npm run copy:assets"
  },
  "dependencies": {

  },
  "devDependencies": {
    "@types/figlet": "^1.5.8",
    "@types/node": "^20.14.9",
    "@types/nodemailer": "^6.4.15",
    "cpx": "^1.5.0",
    "nodemon": "^3.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.2"
  }
}